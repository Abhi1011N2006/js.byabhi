const choice = "name"

switch (choice) {
    case "name":
        console.log(`name is : ${"ABHI"}`);
        break;
    case "roll":
        console.log(`roll no is : ${32}`);
        break;
    case "username" :
        console.log(`username is : ${"abhi123xyz"}`);
        break;
    default:
        console.log("it is default statement");
        break;
}